<!DOCTYPE html>
<html>
	<head>
		<title>SHOP PROJECT</title>
		<link rel="stylesheet" href="https://fonts.cdnfonts.com/css/roboto" />
		<link
			rel="stylesheet"
			href="/<%= process.env.ADMIN_PATH %>/css/normalize.css"
		/>
		<link
			rel="stylesheet"
			href="/<%= process.env.ADMIN_PATH %>/css/style.css"
		/>
	</head>
	<body>
		<header class="header">
			<div class="logo"></div>
			<div class="header-buttons">
				<% if (user) { %>
				<a href="<%= `/${process.env.ADMIN_PATH}/new-product` %>"
					>Add product</a
				>
				<a href="<%= `/${process.env.ADMIN_PATH}/auth/logout` %>">LOGOUT</a>
				<% } %>
			</div>
		</header>
		<div class="container"><% if (user) { %> <% } %> <%- body %></div>
	</body>
</html>
